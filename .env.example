# For running the bot locally (python3 plaza_alert.py) instead of via GitHub Actions.
# Copy this file to .env and fill in your value. .env is gitignored.
#
# Gmail App Password for myhousingbot@gmail.com (NOT the normal login password).
# Create one at https://myaccount.google.com/apppasswords
GMAIL_APP_PASSWORD=
